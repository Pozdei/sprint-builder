# Jira credentials
JIRA_BASE_URL=https://itlime.atlassian.net
JIRA_EMAIL=pozdeev.sa@lime-shop.com
JIRA_API_TOKEN=положи_свой_токен_сюда

# БД (Postgres локально). Если меняешь — поправь и в createdb команде.
DATABASE_URL=postgresql+psycopg://sprint_app:sprint_local_dev@localhost:5432/sprint_builder

# Прокси/SSL — заполнить если нужно
# HTTP_PROXY=http://proxy.corp.local:8080
# HTTPS_PROXY=http://proxy.corp.local:8080
# REQUESTS_CA_BUNDLE=/path/to/corp-ca.pem
# VERIFY_SSL=true
