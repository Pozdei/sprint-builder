# Production-конфигурация.
#
# Сервисы:
#   postgres — БД, данные в named volume "pg_data".
#   backend  — FastAPI, миграции автоматом, ждёт postgres.
#   frontend — Caddy + собранный Vite-фронт. Принимает HTTP/HTTPS снаружи.
#
# Запуск:
#   docker compose up -d --build
#
# Остановка (без удаления данных):
#   docker compose down
#
# Сброс ВСЕХ данных:
#   docker compose down -v       # снимает и volume pg_data

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 20

  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

      # Jira
      JIRA_BASE_URL: ${JIRA_BASE_URL}
      JIRA_EMAIL: ${JIRA_EMAIL}
      JIRA_API_TOKEN: ${JIRA_API_TOKEN}

      # Auth
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRE_HOURS: ${JWT_EXPIRE_HOURS:-24}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}

      # Telegram — глобальный бот-fallback (per-конфиг токен задаётся в UI)
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}

      # AI-срез — глобальные фолбэк-ключи (per-конфиг ключ задаётся в UI)
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      # AI-срез — провайдер "local": общий внутренний OpenAI-совместимый эндпоинт
      AI_LOCAL_BASE_URL: ${AI_LOCAL_BASE_URL:-}
      AI_LOCAL_API_KEY: ${AI_LOCAL_API_KEY:-}
      AI_LOCAL_MODEL: ${AI_LOCAL_MODEL:-Qwen3.8-27B-FP8}

  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      DOMAIN: ${DOMAIN:-:80}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config

volumes:
  pg_data:
  caddy_data:
  caddy_config:
